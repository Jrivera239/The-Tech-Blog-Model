{{> posts post}}

{{#if loggedIn}}
<form class="comment-form black">
<div>
    <textarea name="comment-body black"></textarea>
</div>
    <div>
    <button type="submit">Add post</button>
    </div>
</form>

{{/if}}
{{> comments post.comments}}
{{#if loggedIn}}
<script src="/js/comment.js"></script>
{{/if}} 